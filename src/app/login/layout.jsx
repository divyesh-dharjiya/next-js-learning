import React from 'react'

const LoginLayout = ({children}) => {
  return (
    <div>
        LoginLayout
        <div>
            {children}
        </div>
    </div>
  )
}

export default LoginLayout